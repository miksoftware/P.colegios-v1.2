; ── Upload limits ──
upload_max_filesize = 20M
post_max_size = 25M

; ── Memory & execution ──
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
max_input_vars = 5000

; ── Temporary files ──
upload_tmp_dir = /tmp
