name: demo-colegio

services:
  php:
    build: .
    container_name: demo-colegio_php
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html
      - ./php-config/custom.ini:/usr/local/etc/php/conf.d/custom.ini
    networks:
      - demo-colegio_network
      - traefik_network
    restart: unless-stopped
    depends_on:
      - mysql
      - redis
    environment:
      - APP_ENV=production
      - APP_DEBUG=false

  mysql:
    image: mysql:8.0
    container_name: demo-colegio_mysql
    restart: unless-stopped
    command:
      - --default-authentication-plugin=mysql_native_password
      - --performance-schema=OFF
      - --innodb-buffer-pool-size=64M
      - --max-connections=50
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: demo-colegio_db
      MYSQL_USER: demo-colegio_user
      MYSQL_PASSWORD: CVCUIl7qbsOGcQsq
    volumes:
      - demo-colegio_mysql_data:/var/lib/mysql
    ports:
      - "3319:3306"
    networks:
      - demo-colegio_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot123"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  redis:
    image: redis:7-alpine
    container_name: demo-colegio_redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - demo-colegio_redis_data:/data
    networks:
      - demo-colegio_network
    healthcheck:
      test: [CMD, redis-cli, ping]
      interval: 10s
      timeout: 5s
      retries: 5

  nginx:
    image: nginx:alpine
    container_name: demo-colegio_nginx
    restart: unless-stopped
    volumes:
      - ./src:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
    networks:
      - demo-colegio_network
      - traefik_network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik_network"
      - "traefik.http.routers.demo-colegio-http.rule=Host(`demo-colegio.miksoftwarecol.com`) || Host(`www.demo-colegio.miksoftwarecol.com`)"
      - "traefik.http.routers.demo-colegio-http.entrypoints=web"
      - "traefik.http.routers.demo-colegio-http.middlewares=demo-colegio-redirect-https"
      - "traefik.http.routers.demo-colegio-https.rule=Host(`demo-colegio.miksoftwarecol.com`) || Host(`www.demo-colegio.miksoftwarecol.com`)"
      - "traefik.http.routers.demo-colegio-https.entrypoints=websecure"
      - "traefik.http.routers.demo-colegio-https.tls=true"
      - "traefik.http.routers.demo-colegio-https.tls.certresolver=letsencrypt"
      - "traefik.http.services.demo-colegio-service.loadbalancer.server.port=80"
      - "traefik.http.middlewares.demo-colegio-redirect-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.demo-colegio-redirect-https.redirectscheme.permanent=true"

networks:
  demo-colegio_network:
    driver: bridge
  traefik_network:
    external: true

volumes:
  demo-colegio_mysql_data:
  demo-colegio_redis_data:
